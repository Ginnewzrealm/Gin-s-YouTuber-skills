# 选题评估方法论存档：YouTube 动态选题评估 Agent 系统落地方案

> 存档日期：2026-09-08
> 来源：用户提供
> 定位裁决：**选题分析技能（xuanti-fenxi）的核心引擎** + 选题探索（xuanti-tansuo）的评分规则来源
> 池驱动确认：本方法即"从选题池中取单个选题进行深度研究"的工具；独立于池的单次评估保留为可选调用模式，结果写回池
> API 策略：YouTube Data API 为主通道（可获取）；vidIQ 为增强层（无则降级 Google Trends/手填）

---

## 一、系统整体工作流架构

系统由 4 个流水线节点组成：

```
选题意图 → ①语义解析与动态词库扩展 → ②动态构建"临时对标池" → ③边界内数据采集与离群值计算 → ④自动化客观评分矩阵 → 评估报告（满分100）
```

## 二、核心节点落地方案

### Node 1: 语义解析与动态词库扩展（LLM Engine）

- **输入**：用户的原始选题意图（如："商业分析 - 瑞幸咖啡怎么赚钱的"）
- **处理逻辑**：Agent 调用大语言模型，根据 YouTube 用户的真实搜索习惯，动态生成长尾词阵列
- **Prompt 逻辑示例**：

```
你是一个 YouTube SEO 专家。请将用户输入的选题拆解为 3 类动态搜索词：
  a. 精确词（如：瑞幸 商业模式）
  b. 场景/痛点长尾词（如：瑞幸 财报 拆解、瑞幸 9.9 价格战 幕后）
  c. 竞品同类词（如：库迪 商业模式、咖啡赛道 商业分析）
请输出 JSON 格式的词库数组。
```

### Node 2: 动态构建"临时对标池"（Dynamic Discovery）

解决"搜索范围无边界"的问题：

1. **种子搜索**：调取 Node 1 生成的词库，调用 YouTube Data API（search.list）检索过去 90 天内发布的视频
2. **频道提取与去噪规则**：
   - 收集搜索结果背后的 Channel ID
   - 清洗规则：自动过滤粉丝数 > 500 万的超级大媒体或官方新闻号（如 CCTV、36氪官方），仅保留 1万 ~ 100万 粉丝级别的自媒体频道
3. **建立临时列表**：按出现频次排序，截取前 20 个最相关的频道，作为本次评估的临时对标池（Temp Competitor Pool）

### Node 3: 边界内数据采集与离群值计算（Data Processing）

在限定好的 20 个临时频道内抓取数据并运行数学计算：

1. **计算基线（Baseline）**：提取 20 个频道各自最近 10 期视频的播放量，计算每个频道的播放量中位数（Mᵢ）
2. **计算 Outlier Score（离群倍率）**：筛选出所有匹配该选题的相关视频，计算：

   ```
   Outlier Score = 该视频实际播放量(V) / 该频道中位数播放量(M)
   ```

3. **数据真实性校验（Anti-Cheating）**：
   - 互动率核验：若 点赞数/播放量 < 0.5%，标记为刷量脏数据，权重降为 0
   - 近期动量（VPH）：若视频发布在 7 天内，计算其每小时新增播放量

### Node 4: 自动化客观评分矩阵（Scoring Algorithm）

```
Final Score = S_outlier × 40% + S_demand × 30% + S_vph × 15% + S_engagement × 15%
```

| 评估子项 | 客观量化标准 | 得分映射（0-100） |
|---------|-------------|------------------|
| 1. 爆款验证 S_outlier | 临时对标池中相关视频的平均 Outlier Score | >3.0 倍 → 100 分；1.5~3.0 倍 → 70 分；<1.0 倍 → 30 分 |
| 2. 需求强度 S_demand | Google Trends 过去 90 天搜索斜率 + vidIQ 搜索量级 | 斜率>0 且高搜索量 → 100 分；平稳长青 → 80 分；斜率向下 → 40 分 |
| 3. 时效动量 S_vph | 近 30 天内相关新视频的平均每小时新增播放 | VPH>100 → 100 分；VPH<10 → 40 分 |
| 4. 观众互动 S_engagement | 样本视频的真实平均互动率（点赞+评论/播放） | >4% → 100 分；<1% → 30 分 |

## 三、本地 Agent 代码核心逻辑示例

（原文此处为 Python 代码块，存档时未随文附上；实现时按上述四节点逻辑编写。）

## 四、最终 Agent 分析报告输出模版

（原文此处为 Markdown 模板，存档时未随文附上；实现时包含：四维得分+加权总分、临时对标池清单、离群倍率明细、反作弊核验结论、立项建议。）

---

## 映射到我们技能体系（存档裁决）

| 落点 | 借用内容 |
|------|---------|
| **xuanti-fenxi 选题分析**（主） | 4 节点引擎 = 分析卡"竞品怎么做+流量"维度的机评内核；对标池 = 竞品清单；离群倍率 = 流量量化；反作弊 = 数据可信度标注 |
| **xuanti-tansuo 选题探索**（辅） | 四维加权 = 选题池"简单评分"的直接规则；探索只跑简化版（维度分+加权总分+入池），不跑深度报告 |

## 三个已裁决的适配点

1. **池驱动**：本方法作为"池内单选题深度体检"存在，结果写回选题池状态字段；独立单次评估为可选模式
2. **依赖策略**：YouTube Data API 为主通道（可获取，Node 2/3 全量机算）；vidIQ 为增强层，无则降级 Google Trends 网页版/人工填数；无 API 时的兜底为 WebSearch 定性档+置信度标注
3. **阈值采纳**：>500万粉过滤规则直接采纳（对标池口径：排除官方大号，看真实自媒体打法）

---

## 补充解读："只有十几个字"的选题怎么办（2026-09-08 第二十轮）

> 用户场景：真实输入往往只有一句话甚至十几个字（如"ChatGPT 应急抢救事件"），如何深入调研？

**Node 1 就是这个问题的答案**——它是整个系统的冷启动机制：

```
十几个字（"ChatGPT 应急抢救事件"）
  → Node 1 LLM 扩词：
      a. 精确词：ChatGPT 宕机事故 / ChatGPT 急救案例
      b. 场景/痛点长尾：ChatGPT 救回心脏骤停患者 / AI 急救靠谱吗 / 大模型误诊风险
      c. 竞品同类词：Claude 医疗应用 / Gemini 健康咨询 / AI 误诊争议
  → Node 2 用 9 个词去搜 → 20 频道临时对标池
  → Node 3 池内算离群倍率（哪些同类视频爆了、爆了几倍）
  → Node 4 四维加权出分（值不值得做）
  → 立项后 ziliaocaiji 六章深挖 → 补全 8 要素分析卡（惊奇点/事实/观点/争议/切入点…）
```

**与 ziliaocaiji 的分工**：本方法管"模糊选题 → 词库 → 数据化评估"（能不能做、竞品怎么做、流量如何）；ziliaocaiji 管"立项后 → 全网深挖 → 六章报告"（事实核实、来龙去脉、可引用素材）。两者接力，不重叠。

**对分析卡 8 要素的供给关系**：扩词的三类方向天然对应——竞品同类词→"已有竞品怎么做"、离群倍率→"流量怎么样"、长尾词检索结果→"惊奇点/事实/观点/争议"的原料。
