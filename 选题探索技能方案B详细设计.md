# 选题探索技能：方案 B 详细设计（雷达引擎 + 多实例）

> 版本：v1.0 · 定稿日期：2026-09-08 · 第二十五轮产出
> 状态：**已拍板，可进 SKILL.md 骨架**
> 前置阅读：《技能建设清单》《工作流架构设计方案》《选题质量方法论存档》《选题评估方法论存档》

---

## 一、需求重述（第二十五轮澄清）

不是"一个频道换垂类"，而是**同一套机制同时跑多个独立雷达实例**：

| 实例 | 主题 | 状态 |
|---|---|---|
| business-story | 商业类故事：公司、企业、组织、人物 | 方法论成熟（Part1+Part2+KW-05/06 全齐） |
| money-opportunity | 赚钱商机：痛点信号、付费意愿、未满足需求 | 待验证 |
| fitness | 健身自媒体：训练/体态/饮食 | 待验证 |
| ai-watch | AI 领域选题 | 待验证 |

四主题"模式上都是一样的"——这个直觉是方案 B 的基石：**Skill 管"怎么跑雷达"（方法论，唯一且共享），数据目录管"为谁跑雷达"（实例，可无限添加）。**

---

## 二、形态决策：为什么 B 而不是 C（软件）

### 三个真实的债（C 方案要付的）

**债 1：迭代摩擦与核心需求结构性冲突**
你的原话："关键词好的继续保持，不好的淘汰，用新的不断测试。"——KW-05/06 需要**高频微调**。软件形态每次调阈值/加维度都是"改后端+重新部署"；Skill 形态改一个 md/json 文件立即生效。多元平台已付过一次学费：设计完成度高（四层漏斗+KW-05/06 文档齐全），但停在半停摆——不是设计错，是工程细节吞噬了迭代精力。

**债 2：软件倾向"定下来"，方法论核心要"活起来"**
Easel（455⭐，浙大+北大出品）反证：重型软件（Web 前端+112 技能+六平台发布）的选题评分仍是**静态七维写死的**——正是你 reject 的方向。软件把东西铸进 schema，你想改评分逻辑就是一次发版；Skill 让画像/词库/阈值永远是可编辑的文件。

**债 3：四主题方法论未验证**
只有 business-story 的画像/评分/KW-06 参数是现成的。其他三个主题怎么评分、阈值多少、KW-06 什么参数——**全都还没跑过**。在算法未定型之前上软件，等于把液体的算法铸进固体的 schema。

### B 的真实软肋（认账）

- 四实例无统一看板，要开四个文件
- 串行跑：一个主题卡住，其他三个等
- 机器睡眠那晚静默丢失（pmset 缓解，不根治）

### 迁移路径已锁死

B 阶段的实例数据格式即 C 阶段的 schema。将来若四主题跑起来且"开四个文件看太烦"，唤醒多元平台当数据平面，**零数据迁移**。

---

## 三、架构总览

```
radar-engine（唯一"真技能"）
├── SKILL.md                  ← 怎么跑一轮雷达（通用流程，≤500 行）
├── EXTEND.md                 ← 引擎级偏好模板（网络层选择/API key 优先级）
├── references/
│   ├── methodology.md        ← 四维评分锚点（固定标尺）+ KW-05/06 算法全文
│   ├── domain-profile-gen.md ← 领域画像生成 prompt（初始化时一次性用）
│   └── card-template.md      ← 入池卡片模板
└── scripts/
    ├── fisherman.py          ← 打分+双层去重（收编自 Part 1 代码）
    └── kw_evolve.py          ← KW-05/06 词库进化器（收编自多元平台算法）

~/radar-instances/            ← skill 目录外，纯数据，git 版本控制
├── business-story/
│   ├── EXTEND.md             ← 主题画像（LLM 一次性生成，人工可审可改）
│   ├── keywords.json         ← 进化器维护的活词库（带命中率统计）
│   ├── topic-pool.md         ← 本地池（状态机主表）
│   ├── radar.db              ← SQLite（KW-05/06 统计 + 向量去重）
│   └── cards/                ← 入池卡片
├── money-opportunity/ ...（同构）
├── fitness/ ...
└── ai-watch/ ...
```

**自包含红线检查**：radar-engine 不引用 radar-instances 内任何文件——它是"被调用方"，实例配置通过调用参数传入。✅

---

## 四、核心机制详设

### 4.1 领域画像（初始化时一次性生成）

**输入**：一句话主题描述（"商业类故事：公司、企业、组织、人物"）

**产出**（LLM 按 `domain-profile-gen.md` prompt 生成，写入实例 `EXTEND.md`）：

```markdown
## 领域画像：business-story
### 冲突信号（本主题下什么算"有冲突"）
- T1: 对赌失败/破产清算/股权冻结/创始人内斗/司法诉讼
- T2: 高管集体离职/公关危机/核心技术泄露/债务违约
- T3: 业绩下滑/裁员/关店/产品停服
### 硬核信源（本主题下去哪里找素材）
- 工商/司法：天眼查/企查查/裁判文书/CourtListener/SEC EDGAR
- 深度报道：36氪/晚点/财经/维基百科/创始人专访
- 讨论热度：雪球/知乎/脉脉/Reddit/X
### 四维评分锚点（该主题下的细则）
- Conflict 80-100: 存在法律/制度层面冲突或重大利益反转
- Conflict 50-79: 明确争议或质疑、重大变化、出乎意料的结果
- Archive 一票否决: 工商/司法/深度报道 <2 项 → 归零
...
```

**关键性质**：画像是**产物不是代码**——md 文件，人工可审可改；换主题 = 换目录重新生成；初值粗糙没关系，进化器会磨准。

### 4.2 四维评分（每晚批量跑）

```
候选主题
  → LLM 语义解析（实体还原+消歧，China GT 压力测试经验）
  → 对照实例画像的锚点评分（Conflict/Archive/Demand/Outlier 各 0-100）
  → 加权总分 = Conflict×30% + Archive×25% + Demand×25% + Outlier×20%
  → ≥75 → 双层去重 → 入池
```

**Outlier 降级声明**：tansuo 阶段 Outlier 用 WebSearch 定性档（"该话题近 90 天有无同类爆款"），置信度标注；YouTube Data API 精算留给 fenxi。

### 4.3 KW-05/06 进化器（收编自多元平台）

**指标体系**（每个画像词独立统计，存 radar.db）：

| 指标 | 公式 |
|---|---|
| 有效率 | 该词 L2 有效命中数 / L1 命中数 |
| 贡献度 | 该词捞到的选题中，状态走到"已发布"的比例 |
| 稳定性 | 近 7 天有效率 / 近 30 天有效率 |
| 综合评分 | (有效率×50% + 贡献度×30% + 稳定性×20%) × 衰减系数 |

衰减：7 天内有命中 1.0 / 14 天 0.8 / 30 天 0.5 / 超 30 天 0.2

**KW-06 三规则**（每周自动评估，产出建议人审）：
- 有效率 <20% 且命中 ≥10 次 → 建议暂停/降权
- 有效率 >70% 且命中 ≥10 次 → 建议提高权重
- 超 30 天无命中 → 建议删除/更换

**新词发现**：每月从"近期高分入池选题"提炼共性信号 → 候选新词以低权重加入测试（KW-06 自然决定生死）。

### 4.4 选题池与状态机（每实例独立）

| 字段 | 说明 |
|---|---|
| 选题名 / 来源 / 日期 | 基础三件套 |
| 四维分 + 总分 | 机评分 |
| 象限标签 | 高受众高差异=优先 等（Part 2 四象限落成字段） |
| 状态 | 候选 → 已立项 → 资料就绪 → 分析完成 → 已脚本 → 已发布 → 已复盘 |
| 命中词 | 该选题被哪些画像词捞到（贡献度统计的输入） |

**排序四层键**：状态 > 总分 > 时新度 > 完整度。数据不全标"候选·待定"沉底。

**双写**：本地 topic-pool.md（机器工作内存+git 备份）↔ 飞书多维表格（人看）。凌晨任务只写本地；飞书同步可在人工查看时增量推送。

### 4.5 定时与可靠性

| 项 | 方案 |
|---|---|
| 触发 | OpenClaw cron，每实例一个（如 05:00 / 05:30 / 06:00 / 06:30 错峰） |
| 机器睡眠 | `pmset repeat wakeorpoweron MTWRF 05:25:00`（需插电） |
| 失败静默 | 跑完无论成败写一行到实例 `last-run.log`；连续 2 天空入池 → 人工告警 |

---

## 五、已收编的外部参照

| 来源 | 收编了什么 | 放在哪 |
|---|---|---|
| 选题质量.md Part 1 | Fishery 公式 + fisherman_engine.py 骨架 | methodology.md + fisherman.py |
| 选题质量.md Part 2 | 人审 checklist + 四象限 + 三要素 → fenxi 用，tansuo 只取四象限字段 | 池字段设计 |
| 动态评估法 | Node 1 语义解析（实体还原+消歧）+ 反作弊思路 | methodology.md |
| 多元信息汇聚平台 | KW-05 三指标 + 综合评分公式 + 衰减规则 + KW-06 三规则 + 新词小流量测试 | kw_evolve.py |
| Easel（ZJU-REAL） | 反向维度思想 + shared scoring-dimensions 统一口径文件模式 + layer 分层标签 | fenxi 借鉴；tansuo 的 methodology.md 即统一口径文件 |
| last30days（EN/CN） | 网络层能力探测 + 降级链 + `--discover` 多腿协议 | EXTEND.md 网络层配置 |

---

## 六、与下游技能的契约接口

```
radar-engine 产出（写实例 topic-pool.md 状态=候选）
      │
      ▼ 人挑一个说"分析这个"（唯一人工介入点）
xuanti-fenxi 读取该选题行 + 调 ziliaocaiji 六章深挖
      │
      ▼ 产出分析卡，人拍板立项 → 状态=已立项
（后续 jiaoben-sop7 → zhizuo-brief → agrici metadata → fupan）
```

fenxi 立项闸门 = 机评分（动态评估法四维）+ 人审 checklist（Part 2 打分表 + Easel 反向维度补充）双轨呈现。

---

## 七、待办与开放问题

| # | 事项 | 状态 |
|---|---|---|
| 1 | 写 radar-engine 的 SKILL.md 骨架 | 方法论输入全部齐备，可开工 |
| 2 | business-story 实例初始化（生成第一版画像） | 等 SKILL.md 完成后做 |
| 3 | pmset 唤醒设置 | 部署时执行 |
| 4 | money-opportunity / fitness / ai-watch 的方法论验证 | 跑起来之后逐个校准画像 |
| 5 | 若四主题稳定运行且"看四个文件太烦" → 唤醒多元平台当看板 | 触发条件明确，非现在 |
