# xuanti-fenxi 选题分析SOP流程图

- 技能名称：xuanti-fenxi 选题分析
- 版本：final
- 状态：reviewed
- 架构：single_skill
- 更新时间：20260908
- 说明：对池内单个选题做深度分析：切入点穷举排序 + 事实核验三分 + C分层闸门立项建议，产出分析卡，人拍板后状态迁移

## 流程图

```mermaid
flowchart TD
    N1(["触发：分析这个选题"]):::start
    N2{"硬前提检查：资料包就绪？"}:::decision
    N3a["调 ziliaocaiji 补采资料"]:::tool
    N3["机评：动态评估法（默认重档）"]:::tool
    N4{"C 分层闸门：硬条件全过？"}:::decision
    N5["事实核验（独立 pass）"]:::process
    N6["切入点穷举+排序+分桶"]:::process
    N7["组装分析卡+无出处扫描"]:::tool
    N8["人审闸门：做/改方向/不做"]:::manual
    N9(["交付+状态迁移"]):::end
    N1 -->|选题指定| N2
    N2 -.->|资料缺失或完整度<60%| N3a
    N3a -->|补采完成| N3
    N2 -->|资料就绪| N3
    N3 -->|机评完成| N4
    N4 -->|硬条件全过| N5
    N4 -.->|任一不过→建议不做（轻量结论，不出分析卡）| N9
    N5 -->|核验完成| N6
    N6 -->|穷举完成| N7
    N7 -->|过扫描| N8
    N7 -.->|扫描发现无出处断言| N5
    N8 -->|做/不做| N9
    N8 -.->|改方向（换切入角度，重跑N6穷举）| N6
    classDef start fill:#E8F5E9,stroke:#43A047,color:#37474F;
    classDef end fill:#E8F5E9,stroke:#43A047,color:#37474F;
    classDef process fill:#E3F2FD,stroke:#1E88E5,color:#37474F;
    classDef decision fill:#FFF8E1,stroke:#F9A825,color:#37474F;
    classDef manual fill:#F3E5F5,stroke:#8E24AA,color:#37474F;
    classDef tool fill:#E0F7FA,stroke:#00838F,color:#37474F;
    classDef output fill:#FFF3E0,stroke:#FB8C00,color:#37474F;
```

## 节点明细

| 编号 | 名称 | 类型 | 阶段 | 责任人 | 输入 | 动作 | 输出 | 工具 | 状态 | 备注 |
|---|---|---|---|---|---|---|---|---|---|---|
| N1 | 触发：分析这个选题 | start | 触发 | Claude | 用户消息、选题池行（状态=候选） | 用户从选题池指定一个选题并要求分析（'分析这个'），读取选题行全部字段 | 选题行数据 |  | 已确认 | 池状态=候选才允许进入；其他状态提示当前阶段。不支持批量——一次一个全深评（Q3确认） |
| N2 | 硬前提检查：资料包就绪？ | decision | 准备 | Claude | 选题行.ziliaocaiji_doc_url、资料完整度字段 | 检查 ziliaocaiji 六章资料包是否存在且完整度≥60% | 就绪 / 缺失清单 |  | 已确认 | 继承 scriptwriter 铁律：资料不够拒绝开工。Q4确认：缺失时自动调ziliaocaiji补采不问；补采失败报缺失清单终止 |
| N3a | 调 ziliaocaiji 补采资料 | tool | 准备 | Claude | 选题、缺失清单 | 调用 ziliaocaiji 技能做六章深挖，产出资料包；池状态迁移为资料就绪 | 资料包（六章报告+信源标注） | ziliaocaiji（软依赖，固定上游） | 已确认 |  |
| N3 | 机评：动态评估法（默认重档） | tool | 机评 | Claude | 选题、资料包 | 默认重档：Node1 语义解析扩词（含实体还原+消歧）→ Node2 建20频道临时对标池（滤>500万粉）→ Node3 YouTube Data API 精算离群倍率+反作弊 → Node4 四维加权。API 限流/不可用时降级轻档（WebSearch 定性+置信度标注，明确标注"定性档"） | 机评四维分+总分、临时对标池清单 | WebSearch、YouTube Data API（可用时精算，降级定性档+置信度标注） | 已确认 | Q1确认：默认重档，限流降级轻档；降级时标注置信度 |
| N4 | C 分层闸门：硬条件全过？ | decision | 闸门 | Claude | 机评结果、资料包 | 硬条件逐项检查：①Archive≥2项硬核信源 ②合规风险（敏感赛道预警）③获得感≥40（反常识点≥1）。任一不过→不出分析卡，直接建议不做并附理由 | 全过 / 未过项+理由 |  | 已确认 | C分层（Q5确认）：硬条件一视同仁不豁免；热点牵出的新主体（如China GT→赛事运营公司）在N1选题精化时自然处理，素材达标即可立项 |
| N5 | 事实核验（独立 pass） | process | 核验 | Claude | 资料包 | 借 fact-check：按五分类（事实断言/统计数字/归属引语/定义/历史声明）提取全部可验证主张 → 三分：已证实(≥2独立源)/待验证(SIFT四步后仍冲突，冲突保持可见)/观点(标归属方)；无源主张→盲区清单。借 source-verification：待验证项走 SIFT，五问核查 | 事实核验表（三分+置信度）、盲区清单+补采建议 |  | 已确认 | 独立 pass 铁律：不与切入点分析混合（借 fact-check 的 F1-F5 原理，交付须达 F5） |
| N6 | 切入点穷举+排序+分桶 | process | 切入点 | Claude | 选题、资料包、核验后事实集、机评对标池 | 穷举全部切入角度 → 三维度评分排序：空白度40%（对标池谁讲过谁没讲，借content-gap命名证据纪律）/凭证密度30%（资料包硬事实计数）/反常识强度30%（'原本以为…结果'点数）→ 分桶 Quick Wins/Strategic Builds/Long-term → 每角度附差异点+优势+劣势 | 切入点穷举表（排序+分桶+优劣） |  | 已确认 | 用户确认的第一核心价值 |
| N7 | 组装分析卡+无出处扫描 | tool | 交付准备 | Claude | 切入点穷举表、事实核验表、机评分、闸门结果 | 按分析卡模板组装 outputs/analysis/<topic>.md；机械防线：扫描全文无出处断言/引语/统计（借 journalism source-attribution-check），发现即退回 N5 补凭证 | 分析卡 v1（过扫描） |  | 已确认 | 防AI编造不靠自觉靠机器。Q6确认：卡面同时呈现闸门三条件✓/✗（主）+四维分小字（辅） |
| N8 | 人审闸门：做/改方向/不做 | manual | 人审 | 用户 | 分析卡 | 人读卡拍板：做（选定切入角度）/ 改方向（说明改什么）/ 不做（附理由可选） | 立项决定、选定切入角度 |  | 已确认 | 全系统唯一人工介入点（设计哲学：机器管流量，人管判断） |
| N9 | 交付+状态迁移 | end | 收尾 | Claude | 立项决定 | 做→状态=已立项（卡片连同切入角度供 jiaoben-sop7 消费）；不做→状态=淘汰（附理由回写池，供 KW-06 进化器统计）；改方向→按反馈重跑 N6 | 池状态更新、下游可消费的分析卡文件 |  | 已确认 |  |
